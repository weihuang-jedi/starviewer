#!/bin/csh -f

 if($# > 0) then
    set flnm = $1
 else
    echo "Usage: $0 your-wrf-filename"
    exit -1
 endif

 set os = `uname -s`

 if("Linux" == ${os}) then
     vglrun nv -wrf $flnm
 else
     nv -wrf $flnm
 endif

