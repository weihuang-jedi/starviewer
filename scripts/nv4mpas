#!/bin/csh -f

 if($# > 0) then
    set flnm = $1
 else
    echo "Usage: $0 your-mpas-filename"
    exit -1
 endif

 set os = `uname -s`

 if("Linux" == ${os}) then
     vglrun nv -mpas $flnm
 else
     nv -mpas $flnm
 endif

